# RefBoard AI 개선 작업 트래커

> 갱신: 2026-07-10. 진행하면서 상태만 바꿔가며 쓰면 됨.
> 상태 기호: ⬜ 대기 / 🔄 진행중 / ✅ 완료 / ⏸️ 보류(나중에)

---

## 지금 순서대로 처리할 것 (Phase 5 착수 전 필수 2개)

### 1. 🔄 팔레트 색상 역할 분류 + 산출물 검증 — **담당: Claude Code**
**문제:** 팔레트 3세트가 짙은 남색·검정·회색 위주로 나옴 (2026-07-09 스크린샷 확인).

**근본 원인:** Phase 2 분석이 문서에 "배경(Background)"이라고 라벨된 색을 역할 무시하고 브랜드 컬러로 채택.

**⚠️ 지시 방향 개정 (2026-07-10, 태안군 케이스 반영):** "배경 라벨 색 폐기"가 아니라 **"색상 역할 분류"**로 정정 —
- 문서에 배경/테마로 명시된 색(#0f172a 등)은 **배경 요구사항으로 분류·보존** → `uiStructure.mode: dark` 유도 신호 + `palette.background` 후보
- brandColors에서는 배경 역할 색 제외, 그 배경과 대비(4.5:1↑)되는 채도 있는 primary 도출
- 문서 명시 레이아웃 요구도 동일 — 컨셉 변주 대상 아닌 제약으로 분리 추출 (AI 추론 제안과 구분하는 "명시 요구사항" 필드 필요, 데이터 모델 변경안 승인 후 구현)
- (b) `palette.ts` 산출물 검증(대비비 + 채도 하한)은 기존대로 진행

**완료 기준:** 태안군 파일 기준 — 다크 배경 유지 + 그와 어울리는 채도 있는 primary가 팔레트에 포함.

### 2. ✅ "펼쳐보기" 빈 내용 버그 — 판정 완료 (2026-07-10)
**원인 확정 (부니 직접 확인):** 데이터는 정상. 요약이 한 줄뿐이라 펼칠 내용이 없는데 버튼이 무조건 노출되는 **화면 로직 결함**. 다음 단계(섹션 확정) 차단 아님 → **게이트에서 해제.**
**수정:** contentSummary가 접힌 높이 초과할 때만 버튼 노출 — 3단계 재구성 지시(아래 4-d)에 포함해 처리.

---

## Phase 5 착수 (위 2개 완료 후)

### 3. ✅ designBasis 필드 추가 — **담당: Claude Code**
`docs/specs/data-model.md`의 `ConceptOption`에 `designBasis: { palette, moodKeywords, typographyDirection }` 명세 반영 완료 (§6, §7 계보 다이어그램 포함).
**코드 구현(normalize.ts, route.ts)은 보류 — Phase 4 실제 구현 시점에 병행 작성.** 지금 짜면 테스트 불가능한 코드가 됨.

### 4. ✅ 스키마 문서 최신화 — **담당: 부니**
`design-system-schema.md` v1.1 → refboard-ai, design-canon 양쪽 레포에 반영 완료.
**→ v1.1.1로 갱신됨, 아래 4-b 참조.**

### 4-b. ✅ 스키마 v1.1.1 패치 (concept 기본 스케일 명문화) — **담당: 부니 + Claude Code**
Phase 5 mock 검증에서 나온 "fallback 근거 없음" 이슈 해결 (2026-07-10).
- §13 신설: rounded/spacing/typography concept 기본 스케일 계약으로 명문화
  - rounded: none 0 / xs 2 / sm 4 / md 8 / lg 12 / xl 16 / full 9999
  - spacing: base-unit 8, section은 domain별 분기(marketing-web·document 80 / dashboard-ops·generic 40 / mobile-app 24)
  - typography: 크기 스케일 확정 (caption 14px 포함, 지난 세션 수정값과 일치)
- **버그 발견·수정:** 렌더러의 기존 rounded 값이 §5.1 일반 예시값(md:12/lg:20)을 쓰고 있어서 §13.1 확정값(md:8/lg:12)과 불일치했음 — §13.1 기준으로 정정
- known-gaps reason을 "schema §13 concept 기본 스케일 적용"으로 통일, 토큰 소스 기반 파생 유지
- `DESIGN-refboard-ai.md`는 internal-md라 렌더러 산출물 아님 — schema-version만 수동 갱신
- `DESIGN-concept-sample*.md` 2개는 렌더러 재생성으로 갱신 (손편집 안 함)
- domain 분기 테스트 5종 추가, 회귀 테스트 총 176개 통과, tsc 클린
- **design-canon 레포 미반영** — 스키마가 며칠 더 손볼 수 있는 상태라 안정화 후 한 번에 동기화하기로 함 (트래커 12번과 합쳐서 처리)

### 4-a. ⬜ DESIGN-refboard-ai.md 레포 반영 — **담당: 부니**
> 트래커 구 버전에 "완료"로 잘못 기록됐던 항목. 정확히는 문서 **작성**만 완료고 **레포 반영**은 별개로 안 끝남.

**현재 상태:** 회사 PC 로컬 다운로드까지만 됨. `refboard-ai` 레포의 `docs/sample/DESIGN-refboard-ai.md`로 아직 안 올라감.
**주의:** 트래커 12번(design-canon 레포 동기화)과는 다른 작업. 이건 제품 A 자체 레포에 넣는 것.

### 4-1. ⬜ 페이지별 기능 테스트 — **담당: 부니** ← 지금 여기

**정정:** 이전에 "1~4단계 테스트 완료"로 기록했던 게 부정확했음. 실제로는 화면 캡처하다 눈에 띈 문제를 우연히 발견한 수준이었고, 시나리오 기반으로 체계적으로 확인한 건 아님.

| 단계 | 실제 상태 |
|---|---|
| 1. 업로드 | ✅ 테스트 완료 |
| 2. 마스킹 검수 | ✅ 테스트 완료 (버그 발견·수정) |
| 3. 분석 결과 | 🔄 **본격 테스트 시작 (2026-07-10)** — 발견 12건, 아래 4-d로 지시화 |
| 4. 레퍼런스·무드 | ⚠️ 깊은 테스트 안 됨 — 팔레트·인포박스 스타일 불일치만 우연히 발견 |
| 5. 컨셉 3안 | ⬜ 착수, 깊은 테스트 전 |
| 6. 디자인 MD | Phase 5 렌더러는 완성, 실배선 전이라 앱에서 테스트 불가 |

**확인 기준(3~5단계 공통):** 각 단계의 핵심 기능이 끝까지 동작하는지(다음 단계로 정상 진행되는지), 특히 5단계는 컨셉 확정 시 ConceptJson이 정상 생성되는지가 핵심. 화면이 안 예쁜 건 안정화 라운드(7~10번)로 미뤄도 됨.
발견되는 버그는 스크린샷으로 공유 → 같이 원인(데이터 결함 vs 화면 결함) 분류.

### 4-d. ⬜ 분석 결과(3단계) 페이지 재구성 — **담당: Claude Code (지시: 부니+Claude 작성 완료)**
4-1 테스트에서 부니가 발견한 12건 통합 지시 (2026-07-10). 요약:
1. 펼쳐보기 조건부 노출 (게이트 2 해소분)
2. 상단 3개 카드 → "분석 요약" 단일 카드 통합 (프로젝트명/한줄설명/프로젝트도메인/화면유형/핵심키워드/신뢰도+근거). "관리자 화면으로 보입니다" 예/아니오 카드는 화면 유형 셀렉트와 중복이라 제거
3. 필드 재정의 — ⚠️ **DomainHint enum 변경 금지** (lib/references.ts + 스키마 §13.2에 배선됨). UI 라벨만 "화면 유형"으로, "프로젝트 도메인"(업무 영역)은 신규 필드. projectType 사용처 조사 후 통합 검토
4. 신뢰도에 판정 근거 키워드 병기 (근거 없는 % 단독 표시 금지)
5. 추가요청 입력란 → 분석 결과 페이지 맨 하단. 마스킹 확정 → 분석 결과 직행 흐름 보장
6. 화면유형/도메인 변경 시: 재생성 사전 안내 + confirm dialog + 사용자 선택 초기화 사전 고지
7. 구성 페이지 위계(페이지 헤더 / compact list / 기본 접힘) + 태그 정리 — contentType vs layoutPattern 축 시각 구분, 비인터랙티브 태그 primary 제거(must 위반 중), X버튼 "이 섹션 제외" 툴팁

**진행 조건:** projectType 조사 결과 + 명시 요구사항 필드 데이터 모델 변경안 보고 → 승인 후 구현.

**✅ 결정 완료 (2026-07-10):**
- **3번 (projectType 재정의):** A안 승인 — 스펙 원의도대로 "산출물 형식"(브로셔/제안서/피치덱/랜딩페이지)으로 복원. 3축 분리 확정: 화면 유형(DomainHint, 유지) / 산출물 형식(구 projectType) / 프로젝트 도메인(업무영역, 신규)
- **0번 (explicitRequirements 타입):** 설계안 승인. `kind: background-color|mode|layout|other` 배열, 컨셉 3안 공통 제약으로 취급. palette.ts DARK_BG 앵커 처리는 미결정 유지(구현 시 별도 논의)
- **7-보류 (공통규칙 페이지 pageKind 분리):** ✅ **A안 확정 (2026-07-11, 부니):** 구조 변경 없이 사용자가 해당 페이지를 수동 제외하는 가벼운 처리로 종결. 코드 변경 없음.

**🔄 실제 필드 스펙 확인됨 — 7번(위계 정리) 지시 구체화 필요:**
페이지 레벨: 체크박스(최대 5개) / `pageRole` 뱃지(8종: 표지·간지·본문·사례·지표·팀·부록·연락처) / 출처 슬라이드번호 / 문서 자체 ID / 미선택 상태 2종(사용자 직접 제외→사유 셀렉트 6종 노출, AI 자동 미선택→안내 문구만)
섹션 레벨: contentType+layoutPattern 태그 / **미결 이슈 뱃지**(문서 리뷰코멘트 보존분 — 현재 화면에 미노출, 노출 필요) / contentSummary(50자 초과 시 펼쳐보기) / X버튼(섹션 제외, 확인됨)

갱신된 지시:
```
7. 구성 페이지 위계·태그 정리
페이지 레벨:
- 페이지명 = 강한 헤더, pageRole 뱃지 옆 배치
- 미선택 상태 시각 구분: "사용자 직접 제외"(사유 셀렉트 노출) vs 
  "AI 자동 미선택"(안내 문구만, 사유 셀렉트 없음) — 지금 섞여 보이는지 확인
- 출처 슬라이드/문서ID는 보조 정보로 축소 표기 (낮은 위계)

섹션 레벨:
- compact list, 기본 3개 요약 + 페이지 단위 펼침
- contentType vs layoutPattern 태그 축 시각 구분
- 미결 이슈 뱃지 노출 (현재 누락)
- contentSummary 50자 초과 시에만 펼쳐보기 (게이트 2 해소)
- X버튼 "이 섹션 제외" 툴팁 필수
- **예외 판단 필드 확정 (2026-07-10):** `recommendedLayout`(layoutPattern) 값이
  화이트리스트("dashboard-layout" 등, 향후 유사 케이스 발견 시 추가)와
  **정확히 일치**할 때만 X버튼 미노출. contentType엔 해당 패턴 안 나옴(확인됨).
  ⚠️ suffix(-layout로 끝나면) 매칭 금지 — "split-layout"처럼 개별 섹션의
  내부 구성을 가리키는 정상 케이스(예: IT 인프라 헬스+이벤트 로그 섹션)까지
  오탐되어 X버튼이 잘못 사라짐. 반드시 특정 값 화이트리스트로 좁혀서 체크.
  (참고: contentType="feature-screen" 관측됨 — 스펙 예시(hero/history/
  business-model/feature/team)에 없는 값, 드리프트 가능성 기록만 해둠·지금 조치 불필요)
- 태그 비인터랙티브 → primary 금지, text-muted+surface-alt로

8. "분석 정보" 카드 해시태그 정리 (7번과 별개 위치 — #스마트시티 #통합관제 등)
- 접기/펼치기 UI 제거. 지금 개수(7개) 수준에선 접을 이유 없음 — 
  기본 다 노출, 개수가 많아질 실사용 사례 나오면 그때 임계값 재검토
- 비인터랙티브 태그: primary 색 금지는 동일 적용하되, 
  locked(비활성) 톤과는 구분되는 읽기 편한 중립톤 유지 
  (예: surface-alt 배경 + text-strong 텍스트 — text-muted보다 진하게)
- 이 해시태그(프로젝트 전역 키워드)와 구성 페이지 내부의 contentType/
  layoutPattern 태그(개별 섹션 성격/표현 태그)는 성격이 다른 두 축임을 
  스타일로 구분 — 같은 회색 배지로 뭉뚱그리지 말 것

9. 마스킹 확정↔분석 시작 흐름 (2026-07-11 재정의, 2026-07-11 레이아웃 확정)
**변경:** 트리거 위치를 분석결과 페이지가 아니라 마스킹 검수 페이지로 이동.
- 마스킹 검수 페이지(2단계) 하단의 "다음" 버튼 제거.
- **레이아웃 확정:** "분석 실행" 섹션 타이틀 제거, 박스(카드 테두리) 제거.
  한 행(flex row, 좌우 배치)에: 좌측 = 인포 아이콘(16px, text-muted) + 
  안내문구(body-sm, text-muted), 우측 = "마스킹 확정하고 분석" 버튼
  (button-primary-xl, 48px, 15번 CTA 배치 규칙 적용 — 하단 우측)
- **안내문구 확정:** `외부로는 마스킹본과 "유지"로 확정한 공개 엔티티 실명만 
  전송됩니다. 확정 후 분석이 시작됩니다.` (버튼 라벨 옆 helper text, 
  4-e 버튼라벨규칙대로 버튼 안엔 정보텍스트 없음)
- 버튼 클릭 1번으로: 마스킹 확정 + Gemini 분석 호출 + 3단계(분석 결과)
  페이지 이동이 한 번에 처리
- 분석 결과 페이지 도착 시엔 로딩 상태("분석 중")만 보이고 버튼 없음
  (16번 loading-state 컴포넌트 적용, 트리거 시점은 이전 페이지 클릭 시점)
- 재방문 시(WorkflowState.analysis 존재) 캐시 활용, 재호출 안 함 — 기존 유지

10. 분석 요약 카드 내부 위계 재정리 (2번 통합 후 확인, 2026-07-10 스크린샷 리뷰)
통합 자체는 완료됐으나 내부가 전부 동일한 회색·비슷한 무게라 스캔이 안 됨.
- 프로젝트명은 heading(22px/700, foreground)로, 한줄설명은 body-lg(text-muted)로
  — 카드 안 첫 블록으로 명확히 구분
- 메타 필드 3종(프로젝트도메인/화면유형/산출물형식): label(body-sm, text-muted) 
  + value는 입력 가능한 요소로 인지되게 input-default 톤 적용 
  (지금은 테두리가 옅어서 비활성처럼 보임 — "비활성처럼 보이면 안 됨" 원칙 위반)
- AI 신뢰도+근거는 하나의 서브 블록으로 묶어 surface-alt 등 배경 채도로 구분
  — "이건 AI 판단 결과"임을 위 메타 필드(편집 가능)와 시각적으로 분리
- 핵심 키워드 "+6" 생략 노출 확인됨 — 8번 지시(접기 제거, 전체 노출)와 배치.
  전부 wrap해서 노출하도록 재확인·수정
- 브랜드 컬러는 라벨+스와치+hex 한 행으로 간결하게
- 블록 간 구분선(border) 또는 배경 채도 차등으로 그룹 경계 표시 
  (하단 "문서 명시 요구사항" 박스의 배경 구분 방식 참고)

11. 프로젝트 도메인 다중값 지원 (2026-07-11)
- 프로젝트는 여러 도메인에 걸칠 수 있음(예: 스마트시티+통합관제+도시운영).
  현재 단일 텍스트 필드로 표시되는데, **먼저 Gemini 응답/타입에서 실제
  몇 개까지 나오는지 확인**(1개 고정인지 가변인지)
- 확인 후 데이터 모델 string → string[]로 변경 (가변 확인되면)
- UI: 값 개수가 가변적이므로 고정폭 grid box 대신 프로젝트 도메인만
  칩/태그 wrap 형태로 분리 표시 (화면유형·산출물형식은 실제로 단일값인지도
  같이 확인해서, 단일이면 기존 select/필드 유지)

12. 섹션 타이틀 위계 통일 + 문서 제목 강조 (2026-07-11)
- "분석 요약" / "문서 명시 요구사항 N건" / "구성 페이지" 3개는 동일 레벨의
  구획 라벨로 통일(같은 무게·크기·아이콘 배치 규칙) — 지금은 서로 다른
  크기라 같은 레벨인지 구분이 안 됨
- "분석 요약" 카드 안의 문서 제목(예: "[Phase B-3] RUC v2 v0 Generation...")은
  heading(22px/700, foreground)으로, 위 3개 구획 라벨보다 뚜렷하게 크게 —
  진입 즉시 "이게 무슨 문서인지" 파악되도록 (10번 위계 정리와 연동)

13. 구성 페이지 체크박스 옆 아이콘 삭제

14. contentType/layoutPattern 원시값 → 사용자 언어 라벨 교체 (2026-07-11)
- "technical-spec", "list", "design-system", "color-palette" 같은 원시 
  slug가 그대로 노출되어 기획자/디자이너도 못 알아봄
- 권장: Gemini 프롬프트에서 contentType/layoutPattern 원시값과 별도로
  "한글 표시 라벨" 필드를 함께 생성 요청 (예: technical-spec→"기술 스펙",
  color-palette→"컬러 팔레트") — enum이 열린 유니온이라 코드 내 고정
  매핑 테이블만으론 신규 값이 계속 새서 유지보수 어려움
- 코드 측 fallback: 라벨 없는 값은 kebab-case를 사람이 읽게 변환
  (하이픈→공백, 첫 글자 대문자)하는 최소 안전장치만 추가

15. 페이지 CTA 배치·크기 (2026-07-11) — ⚠️ 인스턴스 MD 수정 필수
- **원인:** DESIGN-refboard-ai.md §6 page-layout에 "CTA(좌측 정렬)"이 계약으로
  박혀 있음 — 초기 화면 추출 시 우연이 규칙으로 굳은 것. 화면만 고치면 재발.
- 인스턴스 MD 수정: page-layout의 CTA 정렬 좌측→**우측**으로 계약 자체 변경
- 배치 규칙: 페이지 레벨 주 CTA는 **하단 우측**. 보조 액션(임시저장류 성격)은
  주 CTA 바로 왼쪽에 나란히(weak/neutral). 파괴적·역방향 액션(삭제/이전)이
  함께 있으면 반대편(좌측)으로 분리 배치 — 게시판 관례(수정삭제 좌/저장 우)
- 크기: 페이지 레벨 CTA는 48px(`button-primary-xl`, 기존 토큰 활용) —
  현재 40px 기본이 페이지 마감 액션으론 작음. 카드/인라인 액션은 기존 40px 유지

16. 로딩/에러 상태 컴포넌트 정의 (2026-07-11, 참고 이미지 기반)
분석 대기·실패 화면 재구성 — known-gaps의 `spinner`(미정의)·`critical-alert`
(미등장) 항목을 이번에 실물로 채움. 분석 외 레퍼런스검색·무드보드·컨셉생성 등
다른 API 대기/실패 화면에도 공통 재사용.

**loading-state (신규):**
- 전체 중앙정렬(수직 스택): 스피너 → 라벨("분석 중", title 무게) → 
  캡션("수십 초 정도 걸릴 수 있어요", body-sm text-muted)
- 스피너 기존보다 확대(32~40px급), primary 색
- 상단 보안 고지문은 같은 블록 안, 로딩 요소보다 작고 옅은 톤(body-sm, 
  text-muted)으로 구분
- **버튼 없음** — 로딩 중엔 액션 없이 캡션 텍스트만

**critical-alert / error-state (신규):**
- 원형 배경(error-weak) + 경고 아이콘(error 색) — known-gaps critical-alert 해소
- 타이틀("분석에 실패했어요")은 error 색상 강조 (실제 오류 상황이라 
  danger-scope 규칙상 정당 사용)
- 설명 텍스트: body, text-muted
- 오류 상세: 발생시간 제거. **실제 실패 원인 한 줄만 동적 표출** — 
  예: "AI 호출 실패 (HTTP 503)". 네트워크 타임아웃/응답 파싱 실패 등 
  원인이 다르면 그 실제 원인으로 교체해서 보여줌 (고정 문구 아님, 
  catch된 에러 종류에 따라 표시 내용 분기)
- 액션 버튼: **"다시 시도" 하나만**(button-primary). "이전 단계로" 버튼 
  및 하단 "문제가 계속되나요?/도움말 보기" 섹션은 제외
- 정렬: 카드 내부 텍스트 흐름 따라 좌측 정렬 — 15번(페이지 레벨 CTA 
  우측배치)과는 별개 컨텍스트(카드 내부 복구 액션이라 페이지 CTA 규칙 
  적용 대상 아님, 혼동 방지용으로 명시)

17. 레퍼런스 키워드 플랫폼별 분리 (2026-07-11, 4단계 테스트 첫 발견)
- **증상:** 전 플랫폼에 동일한 일반 키워드가 재사용됨 (예: Behance에
  "AI 빅데이터 지식 그래프 온톨로지 브로셔 사례 연구" 같은 부적합 쿼리)
- Claude Code의 감사 문서 진단은 승인, 단 구현 스펙으로 보완 요구:
  ① 플랫폼 프로필(lib/references.ts, 기계가독 구조: language/queryStyle/
  길이상한/good·bad예시)을 SSoT로 — 프롬프트와 후처리가 같은 프로필 참조
  ② 언어 규칙 필수(한국 플랫폼=ko, 글로벌=en) — 감사 문서의 Behance
  한국어 예시는 오류, 영어로 정정
  ③ 변환 위치(프롬프트 생성 vs 후처리) 택일 + 병합 우선순위 정의
  ④ 표 정리(행 깨짐/누락 복구, Fonts in Use 추가, 플랫폼 고유명사
  원문 표기 — "새로운 브랜드"(Brand New) 같은 기계번역 금지)
  ⑤ 완료 기준: 태안 문서 기준 Behance/Dribbble/GDWEB 쿼리가 서로 다른
  어휘·언어로 생성 + 실검색 스팟체크
```

### 4-e. ✅ 스키마 규칙 신설 — "버튼 라벨=행동만, 정보 텍스트 금지" 등 — **담당: 부니(승인 완료) + Claude Code(반영 필요)**
**배경:** "맞습니다/아닙니다 버튼 안 설명"(4-d 2번에서 카드째 제거) → "분석 중... (수십 초 걸릴 수 있음)"(9번) 등 **같은 패턴이 화면마다 반복 발견됨.** 개별 화면 수정만으로는 재발 방지가 안 됨 — 스키마 계약에 규칙으로 명문화 필요.

**추가할 constraint (`design-system-schema.md` §7 + `DESIGN-refboard-ai.md` 인스턴스 §7 양쪽):**
```
id: button-label-action-only
severity: must
scope: action
rule: "버튼 라벨은 사용자가 취할 행동을 가리키는 짧은 문구여야 한다. 
       표현 방식(다시보기/확인하기/다음으로/목록보기 등 어미·스타일)은 자유. 
       단 소요시간·조건·부연설명 등 '정보성 텍스트'는 라벨에 절대 포함하지 
       않고 버튼 옆/아래 helper text(body-sm, text-muted)로 분리한다."

id: button-label-consistency
severity: should
scope: action
rule: "같은 역할을 하는 버튼(상세 이동, 목록 이동, 재조회 등)은 프로젝트 
       전체에서 하나의 표기 스타일로 통일한다. 예: '다시보기/목록보기'처럼 
       동사+보기 형태로 갈지, '목록'처럼 명사형으로 갈지 초반에 정하고, 
       화면마다 스타일이 섞이지 않게 한다."

id: cta-placement
severity: must
scope: layout
rule: "페이지 레벨 주 CTA는 하단 우측 배치, 크기 48px(button-primary-xl). 
       보조 액션은 주 CTA 좌측에 나란히(weak/neutral). 파괴적·역방향 액션이 
       함께 있으면 반대편(좌측)으로 분리한다."
```

**참고:** 이 4-e 규칙들은 "배치·구성·관례" 층 — 기존 스키마가 계약하지 않던 영역.
발견될 때마다 개별 화면 수정으로 소모하지 말고 이렇게 constraint로 승격하는 것이
재발 방지의 유일한 레버 (인스턴스 §7 + 스키마 권장 constraint 세트에 반영).
이후 화면 리뷰 시 이 규칙 하나로 재발 여부를 기계적으로 체크 가능해짐.

### 4-c. 🔄 Phase 5 렌더러 방어 버그 수정 — **담당: Claude Code**
2차 코드 리뷰(2026-07-10)에서 발견, 교차 검증 완료. 팔레트 데이터 품질과 무관한 독립 버그라 게이트 1과 상관없이 지금 진행 승인.
- ⬜ **YAML frontmatter 인젝션** — `lib/render/designMd.ts:596` `name: ${instance.meta.name}`이 이스케이프 없이 삽입됨. 프로젝트명에 `:`, `#`, 줄바꿈 있으면 YAML 파싱 깨짐(실제 재현됨, status 키 중복 등). 이스케이프/따옴표 처리 필요.
- ⬜ **잘못된 hex 색상값 무검증 통과** — `lib/render/designMd.ts:119` hex 파서가 `parseInt` 실패 시 NaN을 비트연산에서 조용히 0으로 처리, "검은색"으로 취급되어 대비 21:1(완벽 통과)로 잘못 기록됨. 현재 내부 파이프라인은 항상 유효 hex만 들어와 실사용 영향 없지만, 외부/저장 데이터 유입 대비 방어 로직 필요.

**⏸️ 실제 앱 연결(Phase 4→5 designBasis 배선)은 별도 — 게이트 1(팔레트 오채택 버그) 결과 확인 전까지 보류.**
`app/api/concept/route.ts`가 지금 팔레트를 프롬프트 텍스트에만 넣고 `ConceptOption.designBasis`로 스냅샷 안 함. `app/page.tsx`의 "디자인 MD" 단계도 아직 플레이스홀더. 이 배선을 지금 완성하면 게이트 1이 안 고쳐진 팔레트를 그대로 계약(designBasis)에 굳히게 되므로, 게이트 1 결과 나온 뒤 착수.

### 5. ✅ Phase 5 렌더러 개발 (mock 데이터 기반) — **담당: Claude Code**
mock ConceptJson → 표준 MD 렌더러 구현 완료 (2026-07-10).
- frontmatter `source: "concept"` / `status: "draft"` 고정
- §12 매핑 규칙대로 palette.background→canvas, palette.text→text 등 반영. on-primary/border는 컨셉에 없는 슬롯이라 `derived` + known-gaps 기록
- 필수 컴포넌트 4종 중 2개(button-primary/card-default)는 concept 레벨로 채움, 나머지 2개는 known-gaps로 대체(에러 없음) — §6.2 완화 규칙대로
- category 7종 순회 의무 면제 반영
- 테스트 15개 추가 (총 156개 전부 통과), `tsc --noEmit` 클린
- 실물 샘플 2개 생성: `docs/samples/DESIGN-concept-sample.md`(A안 라이트), `DESIGN-concept-sample-dark.md`(B안 다크) — light/dark에 따라 on-primary·border derived 값이 다르게 나오는 것까지 확인됨
- 작업 로그 + 프로젝트 메모리 저장 완료

**다음 확인할 것 (부니):** 샘플 2개 §0~§12 구조 검증 — 특히 다크/라이트 derived 값 실제 분기 여부, known-gaps 사유 기록 상태.

### 6. 🔄 Phase 5 테스트 — **담당: 부니 + 나**
mock 기반 형식 검증 절반 진행 (위 5번 샘플 확인 중). 이후 Phase 4 실제 구현 시 designBasis 로직 연결 → 실제 컨셉으로 재검증. 스키마 draft→confirmed 최종 전환 관문.

**Phase 4 실배선 착수 시점:** 지금 바로 안 함. 순서: ① 게이트 2개(1·2번) 결과 확인 → ② 4-1 페이지별 기능 테스트로 컨셉 확정 흐름 자체 점검 → ③ 그 다음 `app/api/concept/route.ts`가 팔레트·무드를 실제로 스냅샷하게 만드는 실배선 착수 여부 결정.

---

## 전체 안정화 라운드 (Phase 5 이후, 시각적 버그 일괄 정리)

### 7. ⏸️ 정보 위계 정리 — **담당: Claude Code (지시는 내가 작성)**
- 분석 결과 화면: 문서 단위/세부 항목/메타태그 3단 위계를 타이포 슬롯(§3)으로 구분
- "프로젝트 정보" 카드의 "브랜드 컬러" 소제목이 섹션 타이틀보다 큰 것 수정
- "이 문서는 다른 사이트 관리자 화면으로 보입니다" 같은 **판단 필요 카드**를 `alert-guide` 컴포넌트로 시각적으로 분리

### 8. ⏸️ 컴포넌트 재사용 불일치 정리 — **담당: Claude Code (지시는 내가 작성)**
- "컬러·무드" 탭과 "섹션별 레퍼런스" 탭의 안내 인포박스 스타일이 서로 다름 → 둘 다 같은 `alert-guide` 토큰 재사용하도록 통일
- 탭 컴포넌트가 탭처럼 안 보이는 어포던스 문제 확인

### 9. ⏸️ 진행 조건 가시성 — **담당: Claude Code (지시는 내가 작성)**
"다음" 버튼 활성화 조건(팔레트 1세트 + 무드 1종 선택 등)을 다 눌러봐야 알 수 있는 문제 — 조건을 미리 화면에 명시하는 패턴으로 개선.

### 10. ⏸️ DESIGN-refboard-ai.md 재추출 — **담당: 나**
전체 안정화 후 실제 화면 기준으로 예시 인스턴스 최종본 재생성.

---

## 별도 트랙 (급하지 않음, 각자 다른 시점)

### 11. ⏸️ data-model.md / CLAUDE.md 문구 통일 — **담당: Claude Code**
스키마 확정 절차 문구를 양쪽 CLAUDE.md에 통일 반영.

### 12. ⏸️ design-canon 레포 동기화 — **담당: 부니 + Claude Code**
`DESIGN-refboard-ai.md`, `design-system-schema.md`(현재 v1.1.1)를 design-canon `docs/sample/`, `schema/`에 실제로 이관.
전제조건: 제품 A 안정화 완료 + design-canon 착수 시점.
**참고:** 스키마가 아직 패치될 가능성 있어 의도적으로 미룬 상태 (4-b 참조) — 안정화 후 최종본으로 한 번에 반영.

### 13. ⏸️ 체크박스 레시피 수치 반영 — **담당: 나**
스키마 §6.6 checkbox-default의 size(20px→16px), border(1.5px→1px) 등을 실측값으로 갱신.

---

## 완료된 것 (기록용)

- ✅ design-system-schema.md v1.0 → v1.1 작성 (이 대화)
- ✅ DESIGN-refboard-ai.md 예시 인스턴스 작성 (이 대화) — *레포 반영은 별개, 4-a 참조*
- ✅ 버튼 대비 버그 3곳 수정 (locked 배경 + primary 텍스트 혼용)
- ✅ scrim 값 변수화
- ✅ canvas/surface/foreground 변수명 정리 (41곳 rename)
- ✅ 컬러피커 버그 수정
- ✅ 그레이 팔레트 — 입력 시드 필터링 (`isUsableBrandColor`) 확인됨 — 단, 산출물 검증 및 근본원인(배경 라벨 색을 브랜드 컬러로 오채택) 조사는 위 1번에서 계속 진행 중
- ✅ designBasis 필드 명세 반영 (data-model.md)
- ✅ design-system-schema.md v1.1 → refboard-ai, design-canon 양쪽 배포
